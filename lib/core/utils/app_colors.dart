import 'package:flutter/material.dart';

class AppColors {
  // Primary & Secondary from Logo (Approximate based on description/visuals)
  static const Color primaryOrange = Color(0xFFFFA726); // Warm Sun
  static const Color primaryBlue = Color(0xFF0277BD); // Trustworthy Blue
  static const Color primaryGreen = Color(0xFF2E7D32); // Nature/Energy

  // Backgrounds
  static const Color scaffoldBackground = Color(
    0xFFF9F9F9,
  ); // Very light grey/white
  static const Color white = Colors.white;

  // Text
  static const Color textPrimary = Color(0xFF333333);
  static const Color textSecondary = Color(0xFF757575);
}
